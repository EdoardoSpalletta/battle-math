<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/yellowish</item>
        <item name="android:statusBarColor">            @android:color/transparent        </item>
        <item name="colorButtonNormal">@color/colorButtonEnabled</item>
ù        <item name="colorOnPrimary">@color/design_default_color_error</item>
        <item name="colorOnBackground"> @color/colorBackgroundImageDT</item>
        <item name="android:colorBackground">@color/colorBackgroundImageDT</item>
    </style>

</resources>